  $(".hoverDiv").click(function () {
            selectedSectionId = $(this).data("id");
            var sectionId = selectedSectionId;
            @* console.log(categoryId); *@
                $(this).addClass("active");

            loadMenuTables(sectionId);
        })
