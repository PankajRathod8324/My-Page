Datepicker (CDN)
html
Copy code
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js"></script>
🔹 Date Picker Inputs
html
Copy code
<input type="text" id="startDate" class="form-control datepicker" placeholder="Select Start Date">
<input type="text" id="endDate" class="form-control datepicker mt-2" placeholder="Select End Date">
🔹 JavaScript:
javascript
Copy code
$(document).ready(function () {
    let today = new Date();

    $("#startDate").datepicker({
        format: "yyyy-mm-dd",
        autoclose: true,
        startDate: today // Disable past dates
    }).on("changeDate", function (e) {
        let startDate = e.format("yyyy-mm-dd");
        $("#endDate").datepicker("setStartDate", startDate);
    });

    $("#endDate").datepicker({
        format: "yyyy-mm-dd",
        autoclose: true
    }).on("changeDate", function (e) {
        let endDate = e.format("yyyy-mm-dd");
        $("#startDate").datepicker("setEndDate", endDate);
    });
});
🎯 Final Outcome
🔹 Solution 1 (HTML5 Date Input) → Simple, no extra libraries.
🔹 Solution 2 (Bootstrap Datepicker) → More user-friendly UI.

Now, when you select a start date, only valid future dates can be selected as the end date and vice versa. 🚀 Let me know if you need any modifications!







