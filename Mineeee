request once and let the controller handle both permission checking and action execution.

🔹 JavaScript Code (Single AJAX Call)
javascript
Copy code
function performAction(url, requestData) {
    $.ajax({
        url: url, // Same URL for both permission check and action
        type: "POST", // Use POST if you're modifying data
        contentType: "application/json",
        data: JSON.stringify(requestData),
        success: function (response) {
            if (response.status === "forbidden") {
                // ❌ User lacks permission, show toast
                showToast("You don't have permission for this action!", "error");
            } else if (response.status === "success") {
                // ✅ Action was successful
                showToast("Action performed successfully!", "success");
                $("#editModal").modal("show"); // Example: Open modal
            } else {
                // ⚠️ Some other unexpected response
                showToast("Unexpected response from server!", "warning");
            }
        },
        error: function () {
            showToast("Something went wrong!", "error");
        }
    });
}

// Generic Toast Function
function showToast(message, type) {
    toastr[type](message); // Types: 'success', 'error', 'info', 'warning'
}
🔹 Usage Example
javascript
Copy code
$(".editBtn").click(function () {
    var id = $(this).data("id");

    // Single AJAX call for both permission check & action
    performAction("/TaxAndFee/Edit", { id: id });
});
✅ Backend Controller (ASP.NET)
Modify your control
