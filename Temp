HTML (Bootstrap 5)
html
Copy code
<!-- Floating Toggle Button (Bottom Right) -->
<button class="btn btn-primary rounded-circle position-fixed" style="bottom: 20px; right: 20px; z-index: 1050;" 
        onclick="toggleOrderTray()">
    <i class="bi bi-list"></i>
</button>

<!-- Bottom Tray Showing Active Orders -->
<div id="orderTray" class="position-fixed bottom-0 start-0 w-100 bg-white border-top shadow p-3" style="display: none; z-index: 1040;">
    <h6 class="mb-2">Active Orders</h6>
    <div id="orderTrayList" class="d-flex gap-3 flex-wrap">
        <!-- Orders will be added here dynamically -->
    </div>
</div>
✅ 2. Bootstrap Icons (optional for list icon)
Add this in your <head> if you're using Bootstrap Icons:

html
Copy code
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
✅ 3. JavaScript
html
Copy code
<script>
    function toggleOrderTray() {
        const tray = document.getElementById('orderTray');
        tray.style.display = tray.style.display === 'none' ? 'block' : 'none';

        if (tray.style.display === 'block') {
            // Simulated active order data
            const orders = [101, 102, 103]; // Replace with real AJAX call

            const listContainer = document.getElementById('orderTrayList');
            listContainer.innerHTML = ''; // Clear previous

            if (orders.length === 0) {
                listContainer.innerHTML = '<div class="text-muted">No active orders</div>';
            } else {
                orders.forEach(id => {
                    const pill = document.createElement('span');
                    pill.className = 'badge bg-primary';
                    pill.textContent = `Order #${id}`;
                    listContainer.appendChild(pill);
                });
            }
        }
    }
</script>
✅ How it Looks:
A circular button at the bottom right (like Swiggy).

