Frontend Code
Include SortableJS and jQuery
html
Copy code
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
HTML List Example
html
Copy code
<ul id="categoryList">
  @foreach (var cat in Model.Categories.OrderBy(c => c.DisplayOrder))
  {
      <li class="category-item" data-id="@cat.Id">
          <span class="drag-icon">
              <i class="fas fa-grip-vertical"></i> <!-- FontAwesome icon -->
          </span>
          @cat.Name
      </li>
  }
</ul>
CSS for Icon
css
Copy code
.drag-icon {
    cursor: grab;
    margin-right: 10px;
}
AJAX + SortableJS
html
Copy code
<script>
    new Sortable(document.getElementById('categoryList'), {
        handle: '.drag-icon',
        animation: 150,
        onEnd: function () {
            let newOrder = [];

            document.querySelectorAll('.category-item').forEach((el, index) => {
                newOrder.push({ id: el.dataset.id, order: index });
            });

            $.ajax({
                url: '/Category/UpdateOrder',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(newOrder),
                headers: {
                    'RequestVerificationToken': $('input[name="__RequestVerificationToken"]').val()
                },
                success: function (res) {
                    console.log("Order updated");
                }
            });
        }
    });
</script>
2. ASP.NET MVC Backend
Model for Order
csharp
Copy code
public class CategoryOrderDto
{
    public int Id { get; set; }
    public int Order { get; set; }
}
Controller Action
csharp
Copy code
[HttpPost]
[ValidateAntiForgeryToken]
public ActionResult UpdateOrder(List<CategoryOrderDto> order)
{
    foreach (var item in order)
    {
        var category = db.Categories.Find(item.Id);
        if (category != null)
        {
            category.DisplayOrder = item.Order;
        }
    }
    db.SaveChanges();
    return Json(new { success = true });
}
3. Optional: FontAwesome Icon Setup
If you're not already using FontAwesome:

html
Copy code
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
Let me know if you want to:
